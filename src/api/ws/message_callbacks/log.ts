import {WsMessageType, messageTypeDict, loggerLevel} from "../../../utils/types/message_type";
import * as WebSocket from "uws";

export default function callback(client: WebSocket, data: WsMessageType) {
    // TODO
}